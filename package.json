{
  "name": "monorepo-fullstack-starter",
  "version": "1.0.0",
  "description": "monorepo starter for fullstack applications",
  "main": "index.js",
  "private": true,
  "engines": {
    "node": ">=24.13.0"
  },
  "scripts": {
    "build": "npm --workspace server run build && npm --workspace client run build",
    "start": "concurrently --names \"server,client\" --prefix name --prefix-colors green,blue \"npm --workspace server run start\" \"npm --workspace client run start\"",
    "dev": "concurrently --names \"server,client\" --prefix name --prefix-colors green,blue \"npm --workspace server run dev\" \"npm --workspace client run dev\"",
    "lint": "npm --workspace server run lint && npm --workspace client run lint",
    "prettier": "npm --workspace server run prettier && npm --workspace client run prettier",
    "format": "npm --workspace server run format && npm --workspace client run format",
    "test": "npm --workspace server run test && npm --workspace client run test",
    "coverage": "npm --workspace server run coverage && npm --workspace client run coverage",
    "clean": "npm --workspace server run clean && npm --workspace client run clean",
    "clean:logs": "npm --workspace server run clean:logs && npm --workspace client run clean:logs",
    "prepare": "husky"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "keywords": [
    "boilerplate",
    "fullstack",
    "monorepo",
    "api-rest",
    "restful",
    "express",
    "react",
    "node",
    "typescript"
  ],
  "author": "Hector Lozano",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "axios": "1.19.0",
    "dayjs": "1.11.23",
    "dotenv": "17.4.2",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@commitlint/cli": "21.2.2",
    "@commitlint/config-conventional": "21.2.2",
    "@commitlint/types": "21.2.0",
    "@eslint/js": "10.0.1",
    "@types/node": "26.2.0",
    "@typescript/native": "npm:typescript@7.0.2",
    "concurrently": "10.0.5",
    "eslint": "10.9.0",
    "eslint-plugin-react-hooks": "7.1.1",
    "eslint-plugin-react-refresh": "0.4.24",
    "globals": "17.11.0",
    "husky": "9.1.7",
    "prettier": "3.9.6",
    "tsx": "4.23.12",
    "typescript": "npm:@typescript/typescript6@6.0.2",
    "typescript-eslint": "8.67.0"
  }
}
